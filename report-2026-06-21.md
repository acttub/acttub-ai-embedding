# 유사 연기영상 검색 — 작업 보고서 (2026-06-21)

> 한 줄 요약: 영상 단일모달 검색에 **음성(CLAP) 모달**과 **"왜 비슷한지" 구간 매칭 설명**을 붙이고, 그 위에 **Gemini AI 코치 피드백 카드(A/B 비교)**까지 얹었다. GitHub 첫 푸시까지 완료.

---

## 0. 이번 세션 큰 줄기

1. **GitHub 첫 커밋 + 푸시** — 코드만, 433MB 영상 데이터 제외
2. **오디오 임베딩(CLAP) + 멀티모달 결합 검색** — 영상+음성 가중 결합, 런타임 비중 조절
3. **"왜 비슷한지 설명" v1 — 구간 매칭** — 추가 모델 0으로 "어느 장면이 닮았는지" 짚기 (app 통합 완료)
4. **Gemini AI 코치 피드백 카드 — A/B 비교** — 입력만 vs 유사 C-pro 동봉, app 통합

전 과정 TDD(테스트 먼저), 최종 테스트 **38개 통과** (GPU 4개 제외).

---

## 1. GitHub 첫 푸시

- 원격: https://github.com/acttub/acttub-ai-embedding (`main` 브랜치)
- `.gitignore`에 `연기영상/`(433MB), `*.log` 추가 → 코드/인덱스/보고서만 푸시
- 시크릿 스캔: 실제 `.env` 없음 (`.venv` 내 라이브러리만, 이미 제외)

## 2. 오디오 임베딩 + 멀티모달

| 추가 | 역할 |
|---|---|
| `audio_utils.load_audio` | PyAV로 영상 → 모노 파형 추출 (48kHz 리샘플) |
| `audio_embedding.AudioEmbedder` | CLAP(`laion/clap-htsat-unfused`) 오디오 임베딩 512d |
| `embedding.combine_embeddings` | 영상+음성 가중 결합 벡터 |
| `multimodal.MultiModalReferenceDB` | 영상/음성 벡터 분리 저장 → 런타임 가중치 결합 검색 |

- **결합 수학**: 각 모달 L2정규화 후 `√w`를 곱해 concat → 결과가 자동 단위벡터,
  코사인이 `(1-w)·영상 + w·음성`으로 깔끔하게 분해됨
- app/CLI에 **음성 가중치 슬라이더/옵션** (0=영상만 · 1=음성만)
- `index.npz` 멀티모달 재구축 (C-pro 34개: video 1024d + audio 512d)

## 3. "왜 비슷한지 설명" — 구간 매칭

핵심 아이디어: V-JEPA가 영상을 단일 벡터로 뭉개기 전에 **시공간 토큰(시간 32 × 공간 256)**을
들고 있다. 전체 평균 대신 **시간축을 구간으로 묶어** 평균내면 추가 추론 없이 구간 임베딩을 얻는다.

| 추가 | 역할 |
|---|---|
| `embedding.embed_segments` | 추론 1회로 구간별 임베딩 `(num_segments, 1024)` |
| `video_utils.get_duration` | 구간 인덱스 → 타임스탬프 환산용 영상 길이 |
| `explain.match_segments` | 질의×기준 구간 코사인 매트릭스 → 가장 닮은 쌍 |
| `explain.explain_match` | 위를 조합해 설명 문장 생성 |

- 출력 예: **"인풋 영상의 10~30초 부근이 C영상의 20~40초 부근과 가장 비슷했어요 (유사도 0.91)"**
- app 검색 결과에 설명 박스 추가
- 사용자 피드백 반영: mm:ss → "초 부근" 부드러운 표현으로 변경

---

## 4. AI 코치 피드백 카드 — Gemini A/B 비교

유사검색은 "닮은 영상"까지만 보여준다. 그 위에 **실제로 뭘 고치면 되는지**를 배우 언어로 짚는 **피드백 카드**를 Gemini로 생성. 원래 스코프 축소로 잔재였던 "Gemini 영상 피드백"을, 이번엔 **유사검색 결과를 활용하는 형태**로 되살렸다.

| 추가 | 역할 |
|---|---|
| `gemini_feedback.build_prompt` | 배우 설정 4개(매체·상황·인물·서브텍스트)를 `prompt.txt` 자리표시자에 채움. 빈 칸은 "비어 있음" 결측 표시 |
| `gemini_feedback.generate_feedback` | 영상 업로드(ACTIVE 대기) → Gemini 호출 → 카드 JSON 파싱. `ref_video` 주면 C-pro를 "전문가 기준선"으로 동봉 |
| `gemini_feedback.parse_feedback_json` | 응답에서 ```json``` 펜스/잡설 벗기고 첫 `{`~마지막 `}`만 파싱 |
| `gemini_feedback.render_card_markdown` | 카드 JSON → 마크다운(강점·관찰·원인·연습·기대효과) |
| `gemini_feedback.load_env` | 외부 의존성 없는 미니 `.env` 로더 (`GEMINI_API_KEY`). 기존 환경변수 안 덮음 |
| `app.compare_feedback` | 유사검색 top-1 C-pro를 찾아 **A(입력만) vs B(C-pro 동봉)** 카드 2장 비교 |

**A/B 비교 설계** — 같은 입력 영상에 두 가지로 피드백:
- **A**: 입력 영상만 주고 피드백
- **B**: 가장 닮은 **C-pro를 "전문가 기준선"으로 함께** 주고 피드백
- B는 `_REF_PREAMBLE`로 "첫 영상은 평가 대상 아닌 기준선, 피드백은 두 번째 배우 영상에만"을 못박아 C-pro를 채점하지 않게 막음. **유사검색이 피드백 품질을 실제로 올리는지** 눈으로 비교가 목적.

**프롬프트(`prompt.txt`) — 5단계 내부 추론**: 관찰 → 진단(과소/과잉 방향 판정) → 카드 초안 → 톤 다듬기 → 전문가 패널 검토를 모델 내부에서 돌리고, **최종 출력은 `overallStrength` + `feedbackCards` JSON 하나**로만 고정(API 계약 유지). 핵심 규칙: 영상에서 "실제로 보이고 들린 것"에만 근거, 어림 타임코드·지어낸 대사 금지. (출처: 우영이 프롬프트 변수명 맞춘 버전)

> ⚠️ **유료 API** — Gemini는 유료 호출이라 **자동 실행 금지**. 코드/테스트만 작성, 실제 호출은 명시 허가 시에만. 테스트는 전부 `_FakeClient` mock으로 **네트워크·과금 0**. `app.py`의 A/B 버튼을 눌러야만 실제 호출.

---

## 5. 기술적 발견 (gotcha)

- **V-JEPA 토큰 8192 = 32(시간) × 256(공간)** — 실측으로 확인(`probe_tokens.py`). 구간 매칭의 근거.
- **CLAP은 10초 초과 입력을 랜덤 구간으로 잘라(rand_trunc) 비결정적** → 10초 청크로 나눠 평균내 회피 + 전체 반영.
- **transformers 5.x API 변경**: `audios=` → `audio=`, `get_audio_features`가 output 객체 반환 → `.pooler_output`(512d, joint space).
- **PyAV 17**: `av.AVError` → `av.FFmpegError`로 개명.
- **uv가 PATH에 없음** → `py -m uv ...`로 호출. `.venv`엔 pip 없음(uv 프로젝트).
- **Gemini SDK는 지연 import** — `from google import genai`를 `_client()` 안에서만. SDK 미설치·키 없어도 모듈 import/테스트 통과(유료 경로 격리).
- **Gemini 영상 입력은 업로드 후 ACTIVE 대기** — `PROCESSING` 동안 폴링하다 ACTIVE 돼야 호출 가능.

---

## 6. 정성 검증 포인트 (열린 항목)

- ⚠️ **토큰 time-major 가정** — `embed_segments`는 토큰 순서가 (시간, 공간)이라 가정.
  V-JEPA 2 표준이지만 100%는 아님. 실제 영상으로 구간 설명이 말 되는지 검증 진행 중.
- 사용자 관찰: 질의/C 구간이 같게 나오는 경우 있으나 정밀 매칭은 비요구사항 (대략 범위면 OK).

---

## 7. 커밋

- `2a8da15` Initial commit: V-JEPA 2 기반 유사 영상 검색 시스템
- `f00f8ae` 오디오 임베딩(CLAP) + 멀티모달 결합 검색 추가
- (미커밋) 구간 매칭 설명(`explain.py`) + app 통합 — working tree
- (미커밋) Gemini AI 코치 피드백 카드(A/B) + `prompt.txt` — working tree

> 구간 매칭·Gemini 피드백은 코드·테스트(38개 통과) 완료, 아직 커밋 전. 다음에 두 덩어리로 나눠 커밋 예정.

## 8. 다음 후보

- 구간 매칭 + Gemini 피드백 두 기능 **커밋 & 푸시**
- 실제 영상으로 **A/B 피드백 정성 비교** — C-pro 동봉이 카드 품질을 올리는지 확인 (유료 API, 허가 후)
- "왜 비슷한지" v2 — CLAP **텍스트 질의**로 음성 속성 설명 ("격앙된 목소리" 등)
- 음성 구간 매칭 ("영상은 이 장면, 음성은 저 구간이 닮음" 분리 설명)
- `build_index.py` 증분 모드
